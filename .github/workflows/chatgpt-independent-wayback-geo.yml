name: ChatGPT Independent Wayback GEO Strategy

on:
  pull_request:
    paths:
      - ".github/workflows/chatgpt-independent-wayback-geo.yml"
      - "tools/chatgpt-share-inventory/run_wayback_geo_strategy.py"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  discover:
    runs-on: ubuntu-latest
    timeout-minutes: 180
    steps:
      - name: Check out collector
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: python -m pip install --disable-pip-version-check -r tools/chatgpt-share-inventory/requirements.txt

      - name: Compile Wayback strategy
        run: python -m py_compile tools/chatgpt-share-inventory/build_inventory.py tools/chatgpt-share-inventory/run_wayback_geo_strategy.py

      - name: Run date-partitioned wildcard discovery
        env:
          INVENTORY_OUT: ${{ github.workspace }}/inventory-output
        run: python tools/chatgpt-share-inventory/run_wayback_geo_strategy.py

      - name: Upload Wayback artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: chatgpt-independent-source-wayback-geo
          path: inventory-output/
          if-no-files-found: error
          compression-level: 6
          retention-days: 1
