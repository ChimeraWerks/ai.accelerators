name: ChatGPT Share Inventory

on:
  pull_request:
    paths:
      - ".github/workflows/chatgpt-share-inventory.yml"
      - "tools/chatgpt-share-inventory/**"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 55
    steps:
      - name: Check out collector
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: python -m pip install --disable-pip-version-check -r tools/chatgpt-share-inventory/requirements.txt

      - name: Compile collector
        run: python -m py_compile tools/chatgpt-share-inventory/build_inventory.py tools/chatgpt-share-inventory/run_bounded.py

      - name: Build bounded metadata-only inventory
        env:
          GITHUB_TOKEN: ${{ github.token }}
          INVENTORY_OUT: ${{ github.workspace }}/inventory-output
        run: python tools/chatgpt-share-inventory/run_bounded.py

      - name: Upload inventory
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: chatgpt-share-inventory-bounded
          path: inventory-output/
          if-no-files-found: error
          compression-level: 6
          retention-days: 1
